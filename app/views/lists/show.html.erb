<div class="d-flex justify-content-between align-items-center mb-4">
    <h1><%= @list.name %></h1>
</div>
  <%# <%= link_to new_list_path do %>
    <%# <button type="button" class="btn btn-primary">Add Movie</button> %>
  <%# <% end %>
  <%= link_to "Add Movie",  new_list_bookmark_path(@list), class: "btn btn-primary" %>

  <ul class="list-group">
    <% @list.movies.each do |movie| %>
      <% bookmark = Bookmark.find_by(list: @list, movie: movie) %>
        <%= movie.title %>
        <%= bookmark.comment %>
        <%= link_to "del", bookmark_path(bookmark), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }  %>
          <%# <div><i class="fa-solid fa-trash"></i></div> %>
        <%# <% end %>
    <% end %>
  </ul>
